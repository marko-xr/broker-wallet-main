class AuthFlags {
  static const bool enableEmailPassword = true;
  static const bool enableGoogle = true;
  static const bool enableApple = true; // iOS only, show if available
  static const bool enablePhone = false; // DISABLED for now
}
